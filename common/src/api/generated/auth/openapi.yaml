# Generated by npm run generate:api-contracts. Do not edit directly.
# Source: api_spec/openapi_v2.yaml

openapi: 3.0.1
info:
  title: sproot
  version: 0.3.0
  description: |
    Sproot is a general purpose environmental management system with a focus on greenhouse and garden. The system is intended to be used to monitor and control various environmental parameters, to  continuously update and maintain "homeostasis". This API is a machine-friendly interface for  interacting with the system, and is designed to be used by a web-based front-end. This specification can be used to generate client libraries, documentation, and other tools.
paths:
  /api/v2/authenticate/token:
    post:
      operationId: authenticateToken
      tags:
        - Authentication
      summary: Get an authentication token for authorization header.
      description: |
        Authenticates a user and returns a JWT token to be used as a Bearer Authorization header. This is intended to be used for API access.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: username
                password:
                  type: string
                  example: password
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            type: object
                            properties:
                              token:
                                type: string
                                example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VybmFtZSI6ImFkbWluIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/authenticate/token
                          details:
                            example:
                              - Missing username
                              - Missing password
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/authenticate/token
                          details:
                            items:
                              example: Invalid username or password.
        "501":
          description: Not Implemented
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 501
                      error:
                        properties:
                          name:
                            example: Not Implemented
                          url:
                            example: /api/v2/authenticate/token
                          details:
                            example:
                              - Authentication is not enabled.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/authenticate/token
                          details:
                            items:
                              example:
                                - Failed to get user from database.
                                - Database connection failed.
  /api/v2/authenticate/login:
    post:
      operationId: authenticateLogin
      tags:
        - Authentication
      summary: Get an authentication token set as a cookie.
      description: |
        Authenticates a user and sets a JWT token as an httponly cookie, and returns a csrf token in the response body. This is intended to be used for web-based front-ends. The csrf token should be included in the x-csrf-token header for all subsequent requests.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: username
                password:
                  type: string
                  example: password
      responses:
        "200":
          description: Success
          headers:
            Set-Cookie:
              schema:
                type: string
                example: jwt_token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VybmFtZSI6ImRldi10ZXN0IiwiY3NyZiI6ImQ4N2MyZGZiLTkyMTUtNGM3Yy04Mzg2LWM4NGU3ODk0ZjRkZSIsImlhdCI6MTcyMDIzOTE1MywiZXhwIjoxOTc5NDM5MTUzfQ.Kp4jyZoBXdrw6dVnk8DPw4hmfWEtFfz0sZoWxBcGjUw; Max-Age=259200; Path=/; Expires=Mon, 01 Jan 2024 00:00:00 GMT; HttpOnly; SameSite=Strict
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            type: object
                            properties:
                              csrf-token:
                                type: string
                                example: 9a2eada2-4e6a-4a41-ae9e-fefaa2475175
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/authenticate/login
                          details:
                            example:
                              - Missing username
                              - Missing password
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/authenticate/login
                          details:
                            items:
                              example: Invalid username or password.
        "501":
          description: Not Implemented
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 501
                      error:
                        properties:
                          name:
                            example: Not Implemented
                          url:
                            example: /api/v2/authenticate/login
                          details:
                            example:
                              - Authentication is not enabled.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/authenticate/login
                          details:
                            items:
                              example:
                                - Failed to get user from database.
                                - Database connection failed.
components:
  schemas:
    SuccessResponse:
      description: |
        A success response object that is returned by all successful API requests.  Contains a status code, timestamp, a unique request Id, and, optionally, some content.
      allOf:
        - type: object
          properties:
            statusCode:
              type: integer
            content:
              type: object
              required:
                - data
        - $ref: "#/components/schemas/ApiResponse"
    ErrorResponse:
      description: |
        An error response object that is returned by all unsuccessful API requests.  Contains a status code, timestamp, a unique request Id, and an error object. This error object may contain the name of the error, and an array of details (if relevant).
      allOf:
        - type: object
          required:
            - error
          properties:
            statusCode:
              type: integer
            error:
              type: object
              properties:
                request:
                  type: object
                  properties:
                    method:
                      type: string
                    url:
                      type: string
                    body:
                      type: object
                    query:
                      type: object
                name:
                  type: string
                url:
                  type: string
                details:
                  type: array
                  items:
                    type: string
        - $ref: "#/components/schemas/ApiResponse"
    ApiResponse:
      description: |
        A generic response object that is returned by all API endpoints. Contains a status code,  timestamp, and a unique request Id.
      type: object
      required:
        - statusCode
        - timestamp
        - requestId
      properties:
        statusCode:
          type: integer
        timestamp:
          type: string
          example: 2021-01-01T00:00:00.000Z
        requestId:
          type: string
          example: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
