/* Generated by npm run generate:api-contracts. Do not edit directly.
 * Source: api_spec/openapi_v2.yaml
 */

import { makeApi, Zodios, type ZodiosEndpointDefinitions, type ZodiosInstance, type ZodiosOptions } from "@zodios/core";
import { z } from "zod";

const authenticateToken_Body = z
  .object({ username: z.string(), password: z.string() })
  .passthrough();
const ApiResponse = z
  .object({
    statusCode: z.number().int(),
    timestamp: z.string(),
    requestId: z.string(),
  })
  .passthrough();
const SuccessResponse = z
  .object({ statusCode: z.number().int(), content: z.object({}).passthrough() })
  .partial()
  .passthrough()
  .and(ApiResponse);
const ErrorResponse = z
  .object({
    statusCode: z.number().int().optional(),
    error: z
      .object({
        request: z
          .object({
            method: z.string(),
            url: z.string(),
            body: z.object({}).partial().passthrough(),
            query: z.object({}).partial().passthrough(),
          })
          .partial()
          .passthrough(),
        name: z.string(),
        url: z.string(),
        details: z.array(z.string()),
      })
      .partial()
      .passthrough(),
  })
  .passthrough()
  .and(ApiResponse);

export const schemas: Record<string, z.ZodTypeAny> = {
  authenticateToken_Body,
  ApiResponse,
  SuccessResponse,
  ErrorResponse,
};

const endpoints: ZodiosEndpointDefinitions = makeApi([
  {
    method: "post",
    path: "/api/v2/authenticate/login",
    alias: "authenticateLogin",
    description: `Authenticates a user and sets a JWT token as an httponly cookie, and returns a csrf token in the response body. This is intended to be used for web-based front-ends. The csrf token should be included in the x-csrf-token header for all subsequent requests.
`,
    requestFormat: "json",
    parameters: [
      {
        name: "body",
        type: "Body",
        schema: authenticateToken_Body,
      },
    ],
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: z
                .object({ "csrf-token": z.string() })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 400,
        description: `Bad request`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 401,
        description: `Unauthorized`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 501,
        description: `Not Implemented`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 503,
        description: `Service unavailable`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/authenticate/token",
    alias: "authenticateToken",
    description: `Authenticates a user and returns a JWT token to be used as a Bearer Authorization header. This is intended to be used for API access.
`,
    requestFormat: "json",
    parameters: [
      {
        name: "body",
        type: "Body",
        schema: authenticateToken_Body,
      },
    ],
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: z.object({ token: z.string() }).partial().passthrough(),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 400,
        description: `Bad request`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 401,
        description: `Unauthorized`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 501,
        description: `Not Implemented`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 503,
        description: `Service unavailable`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
]);

export const authApi: ZodiosInstance<typeof endpoints> = new Zodios(endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): ZodiosInstance<typeof endpoints> {
  return new Zodios(baseUrl, endpoints, options);
}
