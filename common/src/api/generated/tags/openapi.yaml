# Generated by npm run generate:api-contracts. Do not edit directly.
# Source: api_spec/openapi_v2.yaml

openapi: 3.0.1
info:
  title: sproot
  version: 0.3.0
  description: |
    Sproot is a general purpose environmental management system with a focus on greenhouse and garden. The system is intended to be used to monitor and control various environmental parameters, to  continuously update and maintain "homeostasis". This API is a machine-friendly interface for  interacting with the system, and is designed to be used by a web-based front-end. This specification can be used to generate client libraries, documentation, and other tools.
paths:
  /api/v2/tags/journals:
    get:
      operationId: listJournalTags
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Get all journal tags.
      description: |
        Returns a list of all journal tags. This includes basic information about each tag, such as name and id.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            items:
                              $ref: "#/components/schemas/SDBJournalTag"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journals
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journals
                          details:
                            items:
                              example:
                                - Failed to get journal tags from database.
                                - Database connection failed.
    post:
      operationId: createJournalTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Add a new journal tag.
      description: |
        Adds a new journal tag to the service. Returns the information used to create the journal tag.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalTagCreateRequest"
      responses:
        "201":
          description: Journal tag created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 201
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/SDBJournalTag"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journals
                          details:
                            example:
                              - "Missing required field: name"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journals
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journals
                          details:
                            items:
                              example:
                                - Failed to add journal tag to database.
                                - Database connection failed.
  /api/v2/tags/journals/{tagId}:
    patch:
      operationId: updateJournalTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Update a journal tag.
      description: |
        Updates a journal tag. This operation changes only the fields provided.
      parameters:
        - in: path
          name: tagId
          required: true
          description: Journal tag Id
          schema:
            type: string
          example: "1"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalTagUpdateRequest"
      responses:
        "200":
          description: Journal tag updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/SDBJournalTag"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            example:
                              - "Missing required field: id"
                              - "Missing required field: name"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/tags/journals/-1
                          details:
                            items:
                              example: Journal tag with ID -1 not found.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            items:
                              example:
                                - Failed to update journal tag in database.
                                - Database connection failed.
    delete:
      operationId: deleteJournalTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Delete a journal tag.
      description: |
        Deletes a journal tag. This operation is permanent and cannot be undone.
      parameters:
        - in: path
          name: tagId
          required: true
          description: Journal tag Id
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Journal tag deleted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            example: Journal tag with ID 1 successfully deleted.
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            example:
                              - Invalid or missing journal tag Id.
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/tags/journals/-1
                          details:
                            items:
                              example: Journal tag with ID -1 not found.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journals/1
                          details:
                            items:
                              example:
                                - Failed to delete journal tag from database.
                                - Database connection failed.
  /api/v2/tags/entries:
    get:
      operationId: listJournalEntryTags
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Get all journal entry tags.
      description: |
        Returns a list of all journal entry tags. This includes basic information about each tag, such as name and id.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            items:
                              $ref: "#/components/schemas/SDBJournalEntryTag"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journal-entries
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journal-entries
                          details:
                            items:
                              example:
                                - Failed to get journal entry tags from database.
                                - Database connection failed.
    post:
      operationId: createJournalEntryTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Add a new journal entry tag.
      description: |
        Adds a new journal entry tag to the service. Returns the information used to create the journal entry tag.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTagCreateRequest"
      responses:
        "201":
          description: Journal entry tag created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 201
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/SDBJournalEntryTag"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journal-entries
                          details:
                            example:
                              - "Missing required field: name"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journal-entries
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journal-entries
                          details:
                            items:
                              example:
                                - Failed to add journal entry tag to database.
                                - Database connection failed.
  /api/v2/tags/entries/{tagId}:
    patch:
      operationId: updateJournalEntryTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Update a journal entry tag.
      description: |
        Updates a journal entry tag. This operation changes only the fields provided.
      parameters:
        - in: path
          name: tagId
          required: true
          description: Journal entry tag Id
          schema:
            type: string
          example: "1"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTagUpdateRequest"
      responses:
        "200":
          description: Journal entry tag updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/SDBJournalEntryTag"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            example:
                              - "Missing required field: id"
                              - "Missing required field: name"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/tags/journal-entries/-1
                          details:
                            items:
                              example: Journal entry tag with ID -1 not found.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            items:
                              example:
                                - Failed to update journal entry tag in database.
                                - Database connection failed.
    delete:
      operationId: deleteJournalEntryTag
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Tags
      summary: Delete a journal entry tag.
      description: |
        Deletes a journal entry tag. This operation is permanent and cannot be undone.
      parameters:
        - in: path
          name: tagId
          required: true
          description: Journal entry tag Id
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Journal entry tag deleted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            example: Journal entry tag with ID 1 successfully deleted.
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            example:
                              - Invalid or missing journal entry tag Id.
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/tags/journal-entries/-1
                          details:
                            items:
                              example: Journal entry tag with ID -1 not found.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service Unavailable
                          url:
                            example: /api/v2/tags/journal-entries/1
                          details:
                            items:
                              example: "Failed to delete journal entry tag: 1"
components:
  schemas:
    SuccessResponse:
      description: |
        A success response object that is returned by all successful API requests.  Contains a status code, timestamp, a unique request Id, and, optionally, some content.
      allOf:
        - type: object
          properties:
            statusCode:
              type: integer
            content:
              type: object
              required:
                - data
        - $ref: "#/components/schemas/ApiResponse"
    SDBJournalTag:
      description: |
        A journal tag object that is stored in the database. Contains all the information needed to  create a journal tag, including name and color of the tag.
      type: object
      required:
        - name
      properties:
        id:
          type: number
          readOnly: true
          example: 1
        name:
          type: string
          example: "Shelf #1"
        color:
          nullable: true
          type: string
          example: "#0000ff"
    ErrorResponse:
      description: |
        An error response object that is returned by all unsuccessful API requests.  Contains a status code, timestamp, a unique request Id, and an error object. This error object may contain the name of the error, and an array of details (if relevant).
      allOf:
        - type: object
          required:
            - error
          properties:
            statusCode:
              type: integer
            error:
              type: object
              properties:
                request:
                  type: object
                  properties:
                    method:
                      type: string
                    url:
                      type: string
                    body:
                      type: object
                    query:
                      type: object
                name:
                  type: string
                url:
                  type: string
                details:
                  type: array
                  items:
                    type: string
        - $ref: "#/components/schemas/ApiResponse"
    JournalTagCreateRequest:
      description: |
        Request body for creating a journal tag.
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: "Shelf #1"
        color:
          nullable: true
          type: string
          example: "#0000ff"
    JournalTagUpdateRequest:
      description: |
        Request body for updating a journal tag. This operation changes only the fields provided.
      type: object
      properties:
        name:
          type: string
          example: "Shelf #1"
        color:
          nullable: true
          type: string
          example: "#0000ff"
    SDBJournalEntryTag:
      description: |
        A journal entry tag object that is stored in the database. Contains all the information needed to  tag a journal entry, including the name and color of the tag.
      type: object
      required:
        - name
      properties:
        id:
          type: number
          readOnly: true
          example: 1
        name:
          type: string
          example: Watering
        color:
          nullable: true
          type: string
          example: "#0000ff"
    JournalEntryTagCreateRequest:
      description: |
        Request body for creating a journal entry tag.
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: Watering
        color:
          nullable: true
          type: string
          example: "#0000ff"
    JournalEntryTagUpdateRequest:
      description: |
        Request body for updating a journal entry tag. This operation changes only the fields provided.
      type: object
      properties:
        name:
          type: string
          example: Watering
        color:
          nullable: true
          type: string
          example: "#0000ff"
    ApiResponse:
      description: |
        A generic response object that is returned by all API endpoints. Contains a status code,  timestamp, and a unique request Id.
      type: object
      required:
        - statusCode
        - timestamp
        - requestId
      properties:
        statusCode:
          type: integer
        timestamp:
          type: string
          example: 2021-01-01T00:00:00.000Z
        requestId:
          type: string
          example: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    cookieAuth:
      type: apiKey
      in: cookie
      name: jwt_token
