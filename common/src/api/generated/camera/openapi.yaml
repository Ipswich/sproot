# Generated by npm run generate:api-contracts. Do not edit directly.
# Source: api_spec/openapi_v2.yaml

openapi: 3.0.1
info:
  title: sproot
  version: 0.3.0
  description: |
    Sproot is a general purpose environmental management system with a focus on greenhouse and garden. The system is intended to be used to monitor and control various environmental parameters, to  continuously update and maintain "homeostasis". This API is a machine-friendly interface for  interacting with the system, and is designed to be used by a web-based front-end. This specification can be used to generate client libraries, documentation, and other tools.
paths:
  /api/v2/camera/settings:
    get:
      operationId: getCameraSettings
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Get camera settings.
      description: Returns the current camera settings.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/CameraSettings"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/camera/settings
                          details:
                            items:
                              example: Invalid or Missing JWT.
    patch:
      operationId: updateCameraSettings
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Update camera settings.
      description: |
        Updates the camera settings. This operation changes only the fields provided.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CameraSettings"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            $ref: "#/components/schemas/CameraSettings"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 400
                      error:
                        properties:
                          name:
                            example: Bad Request
                          url:
                            example: /api/v2/camera/settings
                          details:
                            items:
                              example:
                                - Missing timelapse enabled
                                - Missing timelapse image retention days
                                - Missing timelapse image retention size
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 401
                      error:
                        properties:
                          name:
                            example: Unauthorized
                          url:
                            example: /api/v2/camera/settings
                          details:
                            items:
                              example: Invalid or Missing JWT.
        "503":
          description: Service unavailable
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 503
                      error:
                        properties:
                          name:
                            example: Service unavailable
                          url:
                            example: /api/v2/camera/settings
                          details:
                            items:
                              example: "Failed to update camera settings: database connection failed."
  /api/v2/camera/stream:
    get:
      operationId: getCameraStream
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Get a live MJPEG camera stream.
      description: Returns a live MJPEG stream from the camera.
      responses:
        "200":
          description: Success
          content:
            multipart/x-mixed-replace:
              schema:
                type: string
                format: binary
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 502
                      error:
                        properties:
                          name:
                            example: Bad Gateway
                          url:
                            example: /api/v2/camera/stream
                          details:
                            items:
                              example: Failed to connect to camera stream
  /api/v2/camera/reconnect:
    post:
      operationId: reconnectCamera
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Reconnect to the camera.
      description: |
        Attempts to reconnect to the camera. This is useful if the camera has lost connection or  is not responding.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            example: Livestream successfully reconnected
        "502":
          description: Bad Gateway
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 502
                      error:
                        properties:
                          name:
                            example: Bad Gateway
                          url:
                            example: /api/v2/camera/reconnect
                          details:
                            items:
                              example: Could not connect to camera server
  /api/v2/camera/latest-image:
    get:
      operationId: getLatestCameraImage
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Get the latest image.
      description: Returns the most recently taken image.
      responses:
        "200":
          description: Success
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        "404":
          description: Image not found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/camera/latest-image
                          details:
                            items:
                              example: No latest image
  /api/v2/camera/timelapse/images:
    delete:
      operationId: clearTimelapseImages
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Clear all timelapse images.
      description: Removes all images from the timelapse directory.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        type: integer
                        example: 200
                      content:
                        properties:
                          data:
                            type: string
                            example: All images cleared successfully
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 409
                      error:
                        properties:
                          name:
                            example: Conflict
                          url:
                            example: /api/v2/camera/timelapse/images
                          details:
                            items:
                              example: Could not clear images at this time. Please try again later.
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 500
                      error:
                        properties:
                          name:
                            example: Internal Server Error
                          url:
                            example: /api/v2/camera/timelapse/images
                          details:
                            items:
                              example: Could not clear all images
  /api/v2/camera/timelapse/archive:
    get:
      operationId: downloadTimelapseArchive
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Get the timelapse archive.
      description: Returns a tar file containing all timelapse images.
      responses:
        "200":
          description: Success
          headers:
            Content-Length:
              description: The size of the content in bytes.
              schema:
                type: string
                example: "12345678"
            Content-Disposition:
              description: Indicates that the content is a file attachment.
              schema:
                type: string
                example: attachment; filename="timelapse.tar"
          content:
            application/x-tar:
              schema:
                type: string
                format: binary
        "404":
          description: Archive not found
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 404
                      error:
                        properties:
                          name:
                            example: Not Found
                          url:
                            example: /api/v2/camera/timelapse/archive
                          details:
                            items:
                              example: No timelapse archive available.
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ErrorResponse"
                  - properties:
                      statusCode:
                        example: 500
                      error:
                        properties:
                          name:
                            example: Internal Server Error
                          url:
                            example: /api/v2/camera/timelapse/archive
                          details:
                            items:
                              example: Error streaming timelapse archive.
  /api/v2/camera/timelapse/archive/regenerate:
    post:
      operationId: regenerateTimelapseArchive
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Regenerate the timelapse archive.
      description: |
        Regenerates the timelapse archive. Useful if you want to regenerate it to now, instead of waiting for the usual interval.
      responses:
        "202":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 202
                      content:
                        properties:
                          data:
                            example: Timelapse archive regeneration queued.
  /api/v2/camera/timelapse/archive/status:
    get:
      operationId: getTimelapseArchiveStatus
      security:
        - bearerAuth: []
        - cookieAuth: []
      tags:
        - Camera
      summary: Get the status of the timelapse archive regeneration.
      description: |
        Returns the status of the timelapse archive regeneration. This is useful to check if the regeneration is still in progress.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessResponse"
                  - properties:
                      statusCode:
                        example: 200
                      content:
                        properties:
                          data:
                            type: object
                            properties:
                              isGenerating:
                                type: boolean
                                example: "true"
                              archiveProgress:
                                type: number
                                example: 75
components:
  schemas:
    SuccessResponse:
      description: |
        A success response object that is returned by all successful API requests.  Contains a status code, timestamp, a unique request Id, and, optionally, some content.
      allOf:
        - type: object
          properties:
            statusCode:
              type: integer
            content:
              type: object
              required:
                - data
        - $ref: "#/components/schemas/ApiResponse"
    CameraSettings:
      description: |
        An object containing the current camera settings.
      type: object
      required:
        - enabled
        - name
        - xVideoResolution
        - yVideoResolution
        - videoFps
        - xImageResolution
        - yImageResolution
        - timelapseEnabled
        - imageRetentionDays
        - imageRetentionSize
        - timelapseInterval
        - timelapseStartTime
        - timelapseEndTime
      properties:
        id:
          type: number
          example: 1
        enabled:
          type: boolean
          example: true
        name:
          type: string
          example: Camera 1
        xVideoResolution:
          nullable: true
          type: number
          example: 1920
        yVideoResolution:
          nullable: true
          type: number
          example: 1080
        videoFps:
          nullable: true
          type: number
          example: 30
        xImageResolution:
          nullable: true
          type: number
          example: 1920
        yImageResolution:
          nullable: true
          type: number
          example: 1080
        timelapseEnabled:
          type: boolean
          example: true
        imageRetentionDays:
          type: number
          example: 30
        imageRetentionSize:
          type: number
          example: 1000
        timelapseInterval:
          nullable: true
          type: number
          example: 60
        timelapseStartTime:
          nullable: true
          type: string
          example: 00:00
        timelapseEndTime:
          nullable: true
          type: string
          example: 23:59
    ErrorResponse:
      description: |
        An error response object that is returned by all unsuccessful API requests.  Contains a status code, timestamp, a unique request Id, and an error object. This error object may contain the name of the error, and an array of details (if relevant).
      allOf:
        - type: object
          required:
            - error
          properties:
            statusCode:
              type: integer
            error:
              type: object
              properties:
                request:
                  type: object
                  properties:
                    method:
                      type: string
                    url:
                      type: string
                    body:
                      type: object
                    query:
                      type: object
                name:
                  type: string
                url:
                  type: string
                details:
                  type: array
                  items:
                    type: string
        - $ref: "#/components/schemas/ApiResponse"
    ApiResponse:
      description: |
        A generic response object that is returned by all API endpoints. Contains a status code,  timestamp, and a unique request Id.
      type: object
      required:
        - statusCode
        - timestamp
        - requestId
      properties:
        statusCode:
          type: integer
        timestamp:
          type: string
          example: 2021-01-01T00:00:00.000Z
        requestId:
          type: string
          example: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    cookieAuth:
      type: apiKey
      in: cookie
      name: jwt_token
