/* Generated by npm run generate:api-contracts. Do not edit directly.
 * Source: api_spec/openapi_v2.yaml
 */

import { makeApi, Zodios, type ZodiosEndpointDefinitions, type ZodiosInstance, type ZodiosOptions } from "@zodios/core";
import { z } from "zod";

const ApiResponse = z
  .object({
    statusCode: z.number().int(),
    timestamp: z.string(),
    requestId: z.string(),
  })
  .passthrough();
const SuccessResponse = z
  .object({ statusCode: z.number().int(), content: z.object({}).passthrough() })
  .partial()
  .passthrough()
  .and(ApiResponse);
const CameraSettings = z
  .object({
    id: z.number().optional(),
    enabled: z.boolean(),
    name: z.string(),
    xVideoResolution: z.number().nullable(),
    yVideoResolution: z.number().nullable(),
    videoFps: z.number().nullable(),
    xImageResolution: z.number().nullable(),
    yImageResolution: z.number().nullable(),
    timelapseEnabled: z.boolean(),
    imageRetentionDays: z.number(),
    imageRetentionSize: z.number(),
    timelapseInterval: z.number().nullable(),
    timelapseStartTime: z.string().nullable(),
    timelapseEndTime: z.string().nullable(),
  })
  .passthrough();
const ErrorResponse = z
  .object({
    statusCode: z.number().int().optional(),
    error: z
      .object({
        request: z
          .object({
            method: z.string(),
            url: z.string(),
            body: z.object({}).partial().passthrough(),
            query: z.object({}).partial().passthrough(),
          })
          .partial()
          .passthrough(),
        name: z.string(),
        url: z.string(),
        details: z.array(z.string()),
      })
      .partial()
      .passthrough(),
  })
  .passthrough()
  .and(ApiResponse);

export const schemas: Record<string, z.ZodTypeAny> = {
  ApiResponse,
  SuccessResponse,
  CameraSettings,
  ErrorResponse,
};

const endpoints: ZodiosEndpointDefinitions = makeApi([
  {
    method: "get",
    path: "/api/v2/camera/latest-image",
    alias: "getLatestCameraImage",
    description: `Returns the most recently taken image.`,
    requestFormat: "json",
    response: z.void(),
    errors: [
      {
        status: 404,
        description: `Image not found`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/camera/reconnect",
    alias: "reconnectCamera",
    description: `Attempts to reconnect to the camera. This is useful if the camera has lost connection or  is not responding.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: z.unknown() }).partial().passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 502,
        description: `Bad Gateway`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/camera/settings",
    alias: "getCameraSettings",
    description: `Returns the current camera settings.`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: CameraSettings.describe(`An object containing the current camera settings.
`),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 401,
        description: `Unauthorized`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "patch",
    path: "/api/v2/camera/settings",
    alias: "updateCameraSettings",
    description: `Updates the camera settings. This operation changes only the fields provided.
`,
    requestFormat: "json",
    parameters: [
      {
        name: "body",
        type: "Body",
        schema: CameraSettings,
      },
    ],
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: CameraSettings.describe(`An object containing the current camera settings.
`),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 400,
        description: `Bad request`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 401,
        description: `Unauthorized`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 503,
        description: `Service unavailable`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/camera/stream",
    alias: "getCameraStream",
    description: `Returns a live MJPEG stream from the camera.`,
    requestFormat: "json",
    response: z.void(),
    errors: [
      {
        status: 502,
        description: `Bad Gateway`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/camera/timelapse/archive",
    alias: "downloadTimelapseArchive",
    description: `Returns a tar file containing all timelapse images.`,
    requestFormat: "json",
    response: z.void(),
    errors: [
      {
        status: 404,
        description: `Archive not found`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/camera/timelapse/archive/regenerate",
    alias: "regenerateTimelapseArchive",
    description: `Regenerates the timelapse archive. Useful if you want to regenerate it to now, instead of waiting for the usual interval.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: z.unknown() }).partial().passthrough(),
        })
        .partial()
        .passthrough()
    ),
  },
  {
    method: "get",
    path: "/api/v2/camera/timelapse/archive/status",
    alias: "getTimelapseArchiveStatus",
    description: `Returns the status of the timelapse archive regeneration. This is useful to check if the regeneration is still in progress.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: z
                .object({
                  isGenerating: z.boolean(),
                  archiveProgress: z.number(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough()
    ),
  },
  {
    method: "delete",
    path: "/api/v2/camera/timelapse/images",
    alias: "clearTimelapseImages",
    description: `Removes all images from the timelapse directory.`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.number().int(),
          content: z.object({ data: z.string() }).partial().passthrough(),
        })
        .partial()
        .passthrough()
    ),
    errors: [
      {
        status: 409,
        description: `Conflict`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
      {
        status: 500,
        description: `Internal Server Error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough()
        ),
      },
    ],
  },
]);

export const cameraApi: ZodiosInstance<typeof endpoints> = new Zodios(endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): ZodiosInstance<typeof endpoints> {
  return new Zodios(baseUrl, endpoints, options);
}
