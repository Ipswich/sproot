/* Generated by npm run generate:api-contracts. Do not edit directly.
 * Source: api_spec/openapi_v2.yaml
 */

import {
  makeApi,
  Zodios,
  type ZodiosEndpointDefinitions,
  type ZodiosInstance,
  type ZodiosOptions,
} from "@zodios/core";
import { z } from "zod";

const ApiResponse = z
  .object({
    statusCode: z.number().int(),
    timestamp: z.string(),
    requestId: z.string(),
  })
  .passthrough();
const SuccessResponse = z
  .object({ statusCode: z.number().int(), content: z.object({}).passthrough() })
  .partial()
  .passthrough()
  .and(ApiResponse);
const ErrorResponse = z
  .object({
    statusCode: z.number().int().optional(),
    error: z
      .object({
        request: z
          .object({
            method: z.string(),
            url: z.string(),
            body: z.object({}).partial().passthrough(),
            query: z.object({}).partial().passthrough(),
          })
          .partial()
          .passthrough(),
        name: z.string(),
        url: z.string(),
        details: z.array(z.string()),
      })
      .partial()
      .passthrough(),
  })
  .passthrough()
  .and(ApiResponse);
const SystemStatus = z
  .object({
    system: z
      .object({ totalDiskSize: z.number(), freeDiskSize: z.number() })
      .partial()
      .passthrough(),
    process: z
      .object({
        uptime: z.number(),
        memoryUsage: z.number(),
        heapUsage: z.number(),
        cpuUsage: z.number(),
      })
      .partial()
      .passthrough(),
    database: z
      .object({
        size: z.number(),
        connectionsUsed: z.number(),
        connectionsAvailable: z.number(),
        pendingAcquires: z.number(),
        pendingCreates: z.number(),
      })
      .partial()
      .passthrough(),
    timelapse: z
      .object({
        imageCount: z.number(),
        directorySize: z.number().nullable(),
        lastArchiveGenerationDuration: z.number().nullable(),
      })
      .partial()
      .passthrough(),
  })
  .partial()
  .passthrough();

export const schemas: Record<string, z.ZodTypeAny> = {
  ApiResponse,
  SuccessResponse,
  ErrorResponse,
  SystemStatus,
};

const endpoints: ZodiosEndpointDefinitions = makeApi([
  {
    method: "get",
    path: "/api/v2/system/backups",
    alias: "listSystemBackups",
    description: `Returns a list of available system backups.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({ data: z.array(z.string()) })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/system/backups/create",
    alias: "createSystemBackup",
    description: `Creates a new system backup and stores it as a gzip-compressed SQL dump file.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: z.unknown() }).partial().passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/system/backups/create/status",
    alias: "getSystemBackupCreateStatus",
    description: `Returns the status of the most recent system backup creation process.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z
            .object({
              data: z.object({ inProgress: z.boolean() }).partial().passthrough(),
            })
            .partial()
            .passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/system/backups/download/:fileName",
    alias: "downloadSystemBackup",
    description: `Downloads a system backup file by its filename.
`,
    requestFormat: "json",
    parameters: [
      {
        name: "fileName",
        type: "Path",
        schema: z.string().describe("The name of the backup file to download."),
      },
    ],
    response: z.void(),
    errors: [
      {
        status: 400,
        description: `Bad request`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
      {
        status: 404,
        description: `Backup not found`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/system/backups/restore",
    alias: "restoreSystemBackup",
    description: `Restores a system backup from an uploaded gzip-compressed SQL dump file.
`,
    requestFormat: "binary",
    parameters: [
      {
        name: "body",
        type: "Body",
        schema: z.instanceof(File),
      },
    ],
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: z.unknown() }).partial().passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 400,
        description: `Bad request`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "post",
    path: "/api/v2/system/power-off",
    alias: "powerOffSystem",
    description: `Powers off the system. This operation is permanent and cannot be undone. Though, it  should come back if you haven&#x27;t changed any docker settings. It&#x27;s intent is more of a &quot;restart&quot; then a &quot;power off&quot;.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: z.unknown() }).partial().passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 503,
        description: `Service unavailable`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
  {
    method: "get",
    path: "/api/v2/system/status",
    alias: "getSystemStatus",
    description: `Returns the current status of the system, including uptime, memory usage, CPU usage, and other relevant information. This is useful for monitoring the health of the system. Uptime is in  seconds, memory metrics are in MB, CPU usage is a percentage, and disk sizes are in MB.
`,
    requestFormat: "json",
    response: SuccessResponse.and(
      z
        .object({
          statusCode: z.unknown(),
          content: z.object({ data: SystemStatus }).partial().passthrough(),
        })
        .partial()
        .passthrough(),
    ),
    errors: [
      {
        status: 500,
        description: `Internal server error`,
        schema: ErrorResponse.and(
          z
            .object({
              statusCode: z.unknown(),
              error: z
                .object({
                  name: z.unknown(),
                  url: z.unknown(),
                  details: z.unknown(),
                })
                .partial()
                .passthrough(),
            })
            .partial()
            .passthrough(),
        ),
      },
    ],
  },
]);

export const systemApi: ZodiosInstance<typeof endpoints> = new Zodios(endpoints);

export function createApiClient(
  baseUrl: string,
  options?: ZodiosOptions,
): ZodiosInstance<typeof endpoints> {
  return new Zodios(baseUrl, endpoints, options);
}
